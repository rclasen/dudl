#/bin/sh

set -e

if [ $# != 1 ]; then
	echo "usage: $0 <filename>" >&2
	exit 1
fi

#case "$1" in
#  */*)
#	echo "pathnames are not supported" >&2
#	exit 1;
#esac

echo "$1" | sed -e "s/[^][A-Za-z0-9äöüÖÄÜß.^,_/'%!&\$(){}+-]/./g;s/\.\.*/./g" \
	| tr '[A-ZÖÄÜ]' '[a-zöäü]'
